This app is built as a single-page application: every "page" (Dashboard,
Add Course, View Courses, Search Course, Total Units, Settings) is a
`<section class="page">` inside `frontend/index.html`, shown/hidden by
`frontend/js/app.js`. This folder is kept for structural completeness /
future expansion (e.g. splitting pages into separate HTML fragments).
